import React from 'react';
import { View } from 'react-native';
import { LabelNone } from './Labels';

const DrawerComponent = () => {
  return (
    <View>
      <LabelNone text={'Drawer'} />
    </View>
  );
};

export default DrawerComponent;
